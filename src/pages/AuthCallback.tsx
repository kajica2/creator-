import React from 'react';
import { AuthCallback } from '../components/AuthProvider';

// Route component for handling OAuth callbacks
export default function AuthCallbackPage() {
  return <AuthCallback />;
}