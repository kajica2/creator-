-- Migration 002: Create empty tables (no data seeding)
-- This migration creates empty tables to avoid ON CONFLICT errors
-- Data can be added manually through the application interface

-- This migration is intentionally left empty to avoid conflicts
-- The tables were already created in migration 001
-- Data can be populated through the application UI